#include<stdio.h>
long long int coin(long long int n)
{
for(i=0;i<=1000000000;i++)
{
if(i<12)
{
arr[i]=i;
}
}
}
int main()
{
long long int n;
while(scanf("%lld",&n)!=EOF);
{
k=coin(n);
printf("%lld",k);
}

return 0;
}