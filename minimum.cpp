#include<cstdio>
#include<cstring>
#include<cstdlib>
#include<algorithm>
#include<vector>
using namespace std;
int main()
{
	int t;
	int dp[6101][6101];
	int str[6101];
	scanf("%d",&t);
	while(t--)
	{
		scanf("%s",&str);
		int len=strlen(str);
		memset(dp,0,sizeof(dp));
		for(i=1;i<len;i++)
		{
			for(j=0,k=i;k<n;k++,j++)
		}
	}
return 0;
}